{
  "name": "bootstrap-test",
  "description": "Include Bootstrap CSS and JS via the jsDelivr CDN.",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "serve --listen 3000"
  },
  "devDependencies": {
    "serve": "^13.0.4"
  }
}